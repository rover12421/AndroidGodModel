pluginManagement {
    repositories {
        maven { url uri("$rootDir/repo") }
        mavenCentral()
    }
}

rootProject.name = "AndroidGodModel"
include ':godModel'
include ':myapp'

include ':hash:base', ':hash:plugin'
include ':nameHash:base', ':nameHash:plugin'

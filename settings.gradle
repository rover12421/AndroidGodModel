pluginManagement {
    repositories {
        maven { url uri("$rootDir/repo") }
        mavenCentral()
    }
}

rootProject.name = "AndroidGodModel"
include ':godModel'
include ':myapp'
include ':hashCore'
include ':hashPlugin'
